// Merge two objects into one
const fName = { a: "ana", b: "bianca", c: "cristina" };
const lName = { x: "anronic", y: "mereuta", z: "mancric" };

let conc = Object.assign(fName, lName);

console.log(conc);
